#include <iostream>
using namespace std;

int main() {
  
  int qntd;
  cin >> qntd;

  int matriculas [qntd];

  for (int i=0; i < qntd; i++) {
    cin >> matriculas [i];
  }
  for (int i=qntd -1; i >= 0; i--) {
    cout << matriculas [i] << " ";
  }
  return 0;
}
